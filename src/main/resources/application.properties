spring.application.name=springbootpractice

# Actuator properties
# `management.endpoints.web.exposure.include` can be set to '*'
management.endpoints.web.exposure.include=*
management.endpoints.web.exposure.exclude=health,info
management.info.env.enabled=true

# Actuator info config
info.app.name=My Spring Boot Practice
info.app.description=A cool practice for me!
info.app.version=1.0.0

# Spring security config
# You can just ignore this config by using default user and password
spring.security.user.name=Mirai1129
spring.security.user.password=0000

# server config
server.port=8888
# This config is make server has the prefix
#server.servlet.context-path=/springbootpractice


# custom config
coach.name=Micky
team.name=The Mouse Team